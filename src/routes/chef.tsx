import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, Quote } from "lucide-react";
import { Button } from "@/components/ui/button";
import { DishCard, Eyebrow, PageFrame, PageHero, Reveal } from "@/components/sv-grand";
import { chefs, dishes, images } from "@/lib/sv-grand-data";

export const Route = createFileRoute("/chef")({ head: () => ({ meta: [{ title: "Our Chefs — SV Grand" }, { name: "description", content: "Meet Chef Narayana, Chef Arjun and Chef Vikram, the culinary team behind SV Grand’s modern Indian table." }, { property: "og:title", content: "Our Chefs — SV Grand" }, { property: "og:description", content: "Three culinary voices shaped by regional Indian kitchens, tandoor and ember." }, { property: "og:type", content: "profile" }, { name: "twitter:card", content: "summary_large_image" }] }), component: ChefPage });
function ChefPage() { const signatures = dishes.filter((dish) => ["nalli-on-ember", "dal-sv-grand", "coastal-catch"].includes(dish.slug)); return <PageFrame><PageHero eyebrow="The hands behind the fire" title={<>Our<br/><span className="text-gold-soft">chefs.</span></>} copy="Three distinct points of view, one generous Indian table." image={images.chefImage}/><section className="px-6 py-28 sm:px-10 lg:px-16 lg:py-40"><Reveal className="mx-auto grid max-w-[1180px] gap-16 lg:grid-cols-[.7fr_1.3fr]"><div><Eyebrow>A culinary collective</Eyebrow><p className="mt-6 font-display text-3xl leading-snug text-ivory">From remembered flavours to a new Indian classic.</p></div><div><p className="font-display text-3xl leading-relaxed text-ivory sm:text-5xl">Three chefs, one table shaped by fire.</p><p className="mt-9 max-w-2xl text-sm font-light leading-8 text-muted-foreground">Chef Narayana leads the kitchen with Chef Arjun and Chef Vikram, bringing distinct regional instincts to every vegetarian and non-vegetarian plate.</p><Quote className="mt-12 size-8 stroke-1 text-primary"/><blockquote className="mt-5 max-w-2xl font-display text-2xl leading-relaxed text-gold-soft">“A guest should recognise the soul of a dish before they notice the hand of the chef.”</blockquote></div></Reveal></section><section className="border-y border-border bg-card px-6 py-24 sm:px-10 lg:px-16"><div className="mx-auto max-w-[1280px]"><Eyebrow>Meet the kitchen</Eyebrow><div className="mt-10 grid gap-8 md:grid-cols-3">{chefs.map((chef) => <article key={chef.name} className="border-t border-border pt-5"><div className="overflow-hidden"><img src={chef.image} alt={chef.name} loading="lazy" width={900} height={1100} className="aspect-[4/5] w-full object-cover transition duration-700 hover:scale-[1.03]"/></div><p className="mt-6 text-[0.58rem] uppercase tracking-[0.2em] text-primary">{chef.role}</p><h2 className="mt-3 font-display text-3xl text-ivory">{chef.name}</h2><p className="mt-4 text-sm leading-7 text-muted-foreground">{chef.story}</p></article>)}</div></div></section><section className="border-y border-border bg-card px-6 py-24 sm:px-10 lg:px-16"><div className="mx-auto max-w-[1280px]"><div className="flex items-end justify-between"><div><Eyebrow>Narayana’s signatures</Eyebrow><h2 className="mt-5 font-display text-4xl text-ivory">Three essential plates.</h2></div><Button variant="luxuryOutline" asChild className="hidden sm:inline-flex"><Link to="/menu">Full menu <ArrowRight/></Link></Button></div><div className="mt-10 grid gap-8 md:grid-cols-3">{signatures.map((dish) => <DishCard key={dish.slug} dish={dish}/>)}</div></div></section></PageFrame>; }